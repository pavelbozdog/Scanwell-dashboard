
<section>
  	<div class="wrap">
		<h2>All Participants</h2>
		<ul class="work">
			<% @participants.each do |p| %>
				<li>
					<a href=/participants/<%= p.unique_id %> rel="external"><%= p.unique_id %></a>
				</li>
			<% end %>
		</ul>
	</div>
</section>